
        
      <div>
        <h2>All my friends</h2>

        <% my_friends = @friends.where(user: current_user)  %>
					<% my_friends.each do |friend|  %>
						<li>
							<div class="card-product">
								<%= cl_image_tag friend.photo.key, height: 400, width: 400, crop: :fit %>
								<div class="card-product-infos">
									<h2>
										<%= friend.first_name  %>
										<%= friend.last_name  %>
									</h2>
									<p>
									  <%= friend.city %>
									  <%= friend.gender %>
										<%= friend.price %>
									</p>
								</div>
							</div>
						</li>
					<% end %>
        </div>